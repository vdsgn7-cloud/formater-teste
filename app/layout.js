import "./globals.css"
export const metadata = { title: "Gerador de Roteiro de Estudo — Cristo Rei", description: "Gere roteiros formatados automaticamente" }
export default function RootLayout({ children }) {
  return <html lang="pt-BR"><body>{children}</body></html>
}
